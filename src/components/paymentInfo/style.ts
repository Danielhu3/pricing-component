import styled from "styled-components";
export const PaymentInfo = styled.div`
    grid-area: payment-select;
    display: flex;
    justify-content: end;
    gap: 0.5rem;

    font-size: 1.2rem;
    
    align-items: center;

`