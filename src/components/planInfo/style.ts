import styled from "styled-components";
export const PlanInfo = styled.div`
    grid-area: plan-info;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: min(2rem);

    
`