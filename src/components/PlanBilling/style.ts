import styled from "styled-components";
export const PlanBilling = styled.div`
    display: flex;
    flex-wrap: nowrap;
    gap: 0.5rem;
    align-items: center;

   grid-area: plan-info;
   justify-self: end;
    
`