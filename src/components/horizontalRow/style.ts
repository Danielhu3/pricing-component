import styled from "styled-components";
export const HorizontalRow = styled.hr`
    grid-area: horizontal-row;
    //card has 5rem padding so, add -5rem padding in left and right to make the hr fill the entire card width
    margin: 0 -5rem;
    border: 0;
    border-top: 1px solid hsl(224, 65%, 95%);;

`